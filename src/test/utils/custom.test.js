const { capitalizing } = require("../../utils");

describe('testing utils capitalizing', () => {
    it('string capitalizing lower', () => {
        // capitalizing()
        // expect(postCountScore(2, 7)).toBe(1);
    });
});